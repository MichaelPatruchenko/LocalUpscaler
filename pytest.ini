[pytest]
# Use an ASCII-only temp directory to avoid OpenCV failures on Windows systems
# where the username contains non-ASCII characters.
addopts = --basetemp=C:/Temp/pytest_upscaler
tmp_path_retention_policy = none
